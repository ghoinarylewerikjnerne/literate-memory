// src/main.rs

#[macro_use]
pub mod define_object;
#[macro_use]
pub mod define_class;
#[macro_use]
pub mod macros;
pub mod experiments;

fn main() {
    // The main function is intentionally left empty.
    // This project is a library of artistic and philosophical experiments
    // in Rust's type system. The artifacts are contained within the
    // `src/experiments` module.
    //
    // Some of these artifacts are intentionally non-compiling and serve
    // as statements about the limits of the language. Therefore, this
    // binary is not intended to be run. The code exists to be read,
    // analyzed, and contemplated.
}
